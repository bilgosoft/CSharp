// ReadLine : Kullanıcıdan bilgi alabilmemizi sağlayan fonksiyondur.
// Not : Klavyeden girilen her veri string bir veridir. Bu yüzden farklı bir tipde veri almak istiyorsak convert işlemi yapmamız gerekir.

// Kullanıcıdan metinsel bir veri alıyoruz.	
string text;
text = Console.ReadLine();

// Kullanıcıdan bir karakter veri alıyoruz.	
char text2;
text = char.Parse(Console.ReadLine());

// Kullanıcıdan integer bir veri alıyoruz.	
int number1;
number1 = int.Parse(Console.ReadLine());

// Kullanıcıdan double bir veri alıyoruz.	
double number2;
number1 = double.Parse(Console.ReadLine());
